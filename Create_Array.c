/*
 * Create an array of <length'> bytes long.  We will use an array of chars to store both chars and integers
 */
#include "Project0.h"
char* Create_Array(int length) {
    static char data[100];
    return data;
}